import General from '@/components/Layout/General'

interface IMangaPageProps {
}

const MangaPage: React.FunctionComponent<IMangaPageProps> = (props) => {
  return <General>
    <div className='flex items-center justify-center w-full h-full text-lg font-semibold text-subtle'>
      Soon™️
    </div>
  </General>
}

export default MangaPage
